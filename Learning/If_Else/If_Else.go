package If_else

func If_else_learning() {

	numero := 10

	// Não deixar na mesma linha e manter as cahves
	if numero > 15 {
		//executa
	} else {
		//executa
	}

	// If com declaração de variavel
	if numero2 := numero + 2; numero2 > 0 {
		//executa
	}

	// A variavel criada dentro do "if init" só existe naquele escopo
}
